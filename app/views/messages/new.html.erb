<h1>New Message</h1>

<%= form_with(model: @message, url: room_messages_path(@room)) do |form| %>
  <div class="field">
    <%= form.text_field :content %>
    <%= form.submit "Send" %>
  </div>
<% end %>

<%= link_to 'Back', @message.room %>
